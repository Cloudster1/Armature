apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "armature.fullname" . }}-env
  labels:
    {{- include "armature.labels" . | nindent 4 }}
data:
  {{- include "armature.env" . | nindent 2 }}
